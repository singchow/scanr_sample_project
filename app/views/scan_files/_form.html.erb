<%= form_for(@scan_file) do |f| %>
  <% if @scan_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scan_file.errors.count, "error") %> prohibited this scan_file from being saved:</h2>

      <ul>
      <% @scan_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="field">
    <%#= f.label :result %><br>
    <%#= f.text_area :result %>
  </div> -->
<div>
    <%= f.file_field :avatar %>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
